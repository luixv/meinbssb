{
  # Global options – use Let's Encrypt STAGING for all automatic HTTPS
  acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
}

# Redirect HTTP to HTTPS
http://meintest.bssb.de {
  redir https://meintest.bssb.de{uri}
}

# Serve Flutter Web App and PostgREST API (HTTPS)
meintest.bssb.de {
  handle /api/* {
    uri strip_prefix /api
    
    @cors_preflight {
      method OPTIONS
    }

    handle @cors_preflight {
      respond "" 204
      header {
        Access-Control-Allow-Origin "*"
        Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, PATCH, DELETE"
        Access-Control-Allow-Headers "Authorization, Content-Type, Accept, Prefer"
        Access-Control-Allow-Credentials "false"
      }
    }

    handle {
      reverse_proxy postgrest:3000 {
        header_up Host {upstream_hostport}
        header_down -Access-Control-Allow-Origin
        header_down -Access-Control-Allow-Methods
        header_down -Access-Control-Allow-Headers
        header_down -Access-Control-Allow-Credentials
      }
      header {
        Access-Control-Allow-Origin "*"
        Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, PATCH, DELETE"
        Access-Control-Allow-Headers "Authorization, Content-Type, Accept, Prefer"
        Access-Control-Allow-Credentials "false"
      }
    }
  }

  # Handle token requests with server-side credential injection
  # IMPORTANT: This must come BEFORE the general /rest/* handler
  handle /rest/zmi/token {
    reverse_proxy token-proxy:3002 {
      header_up Host {upstream_hostport}
    }
    header {
      Access-Control-Allow-Origin "*"
      Access-Control-Allow-Methods "POST, OPTIONS"
      Access-Control-Allow-Headers "Authorization, Content-Type"
    }
  }

  # Redirect all other /rest requests to internal web server
  handle /rest/* {
    redir https://webintern.bssb.bayern:56400{uri} permanent
  }

  # Handle email service requests on /email path
  handle /email/* {
    uri strip_prefix /email
    reverse_proxy email-service:3001
    header {
      Access-Control-Allow-Origin "*"
      Access-Control-Allow-Methods "POST, OPTIONS"
      Access-Control-Allow-Headers "Authorization, Content-Type"
    }
  }

  # Serve Flutter Web App (default handler)
  handle {
    root * /web
    file_server
    try_files {path} /index.html
  }
}

# ZMI Monitor on 8083 (internal only, no SSL needed from browser)
meintest.bssb.de:8083 {
  reverse_proxy zmi-monitor:80
}
